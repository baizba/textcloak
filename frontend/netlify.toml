[build]
  command = "npm install && npm run build"
  publish = "dist/textcloak/browser"

[build.environment]
  NODE_ENV = "development"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
