[build]
  command = "npm run build"
  publish = "dist/textcloak/browser"

[build.environment]
  NODE_ENV = "production"
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
